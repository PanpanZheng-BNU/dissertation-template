% !TeX root = ../manuscript.tex

\chapter{补充内容}
\section{插图}
\begin{figure}
\centering
\includegraphics[width=\linewidth]{clustring/horizontal_legend_comparison.pdf} % 替换为你的文件名
\label{fig:clustering_all_comparison}
\caption{不同数据集下全网络平均集聚系数的零模型分布与真实值对比。
图中每一行代表不同的数据集（dataset1 至 dataset8），每一列代表一种聚类系数（从左至右依次为 $\bar{C}^{*, \text{tot}}$、$\bar{C}^{*, \text{cyc}}$、$\bar{C}^{*, \text{mid}}$、$\bar{C}^{*, \text{in}}$ 和 $\bar{C}^{*, \text{out}}$）。蓝色直方图显示了基于拓扑重连的零模型分布，橙色直方图显示了基于保持原有连接但重新随机分配权重的零模型分布。红色虚线标记了原始网络中观测到的真实值。}
\end{figure}

% 附录中的插图示例（图~\ref{fig:appendix-figure}）。

% \begin{figure}
%   \centering
%   \includegraphics[width=0.6\linewidth]{example-image-a.pdf}
%   \caption{附录中的图片示例}
%   \label{fig:appendix-figure}
% \end{figure}


\section{表格}

% 附录中的表格示例（表~\ref{tab:appendix-table}）。



\section{数学表达式}

% 附录中的数学表达式示例（式\eqref{eq:appendix-equation}）。


% \section{文献引用}
